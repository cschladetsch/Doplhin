#!/bin/sh

folder=$1
mkdir $folder && cd $folder
cp ../PromptTemplate.md prompt.md
git add prompt.md && git commit -m "Created $1."

echo
echo "*** Please now use: 'cd $folder' before continuing"
echo

